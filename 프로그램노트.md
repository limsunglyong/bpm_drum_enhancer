# 프로젝트 명칭: [Music Beat Generator]

## 현재 목표
- 모든 작업이 완료되었습니다.

- [x] bpm-analyzer-code2.html 파일에서 찾은 BPM으로 Drum beat를 생성해주고 기존의 음원과 믹싱하여 들려주는 기능을 구현함.
- [x] bpm-analyzer-code2.html 메인 화면에서 mp3 waveform 화면 밑에 drum beat waveform을 표시하는 기능을 구현함.
- [x] Waveform 표시 및 WebAudio api 호출을 메인 스레드에서 수행함.
- [x] 기존 mp3 waveform의 zoom, scroll 기능을 drum beat waveform에도 동일하게 적용함.
- [x] Playhead가 화면 왼쪽 밖으로 벗어났을 때 알려주는 화살표 오버레이 및 복구 기능 구현.

## 기술 스택
- HTML5, CSS3, JavaScript (Antigravity 환경)

## AI 지시사항 (Prompting)
- 코드를 수정할 때 반드시 이 섹션의 지시를 따를 것.
- UI는 현대적인 디자인을 유지할 것.

## 변경 이력 (Changelog)
- `bpm_analysis/bpm-analyzer-code2.html`: 왼쪽 메뉴(MENU) 섹션을 제거하고, 로딩된 MP3 파일에서 ID3 태그를 추출하여 앨범 아트(Album Art)와 곡 정보(Title, Artist, Album, Year)를 표시하는 UI로 교체함. (`jsmediatags` 라이브러리 연동)
- `bpm_analysis/bpm-analyzer-code2.html`: 분석된 BPM에 맞춰 드럼 비트를 합성(Kick/Snare)하고 생성하는 기능을 추가함. MP3 파형 아래에 동기화된 드럼 비트 파형 표시 기능을 구현하였으며, 드럼 믹스 볼륨 조절 슬라이더를 추가함. Zoom 및 Scroll 기능이 두 파형에 모두 동기화되도록 처리함.
- `bpm_analysis/bpm-analyzer-code2.html`: 드럼 비트 타이밍 동기화 기능을 대폭 보강함. 곡 초반 10초를 분석하여 첫 번째 다운비트를 찾는 위상(Phase) 보정 로직을 추가하고, `AudioContext.currentTime`을 이용한 스케줄링 재생 방식을 도입하여 두 소스 간의 정밀한 동기화를 구현함.
- `bpm_analysis/bpm-analyzer-code2.html`: 드럼 비트의 타이밍을 수동으로 미세 조정할 수 있는 **Drum Sync** 슬라이더를 추가함. 사용자가 슬라이더를 조절하면 드럼 파형이 실시간으로 좌우로 밀리며 시각적으로 피드백을 제공하고, 재생 시 해당 오프셋(±200ms)이 정확히 반영되도록 처리함.
- `bpm_analysis/bpm-analyzer-code2.html`: 드럼 사운드(Kick, Snare)를 단일한 **메트로놈 비프음**으로 통합함. 또한, 푸터 레이아웃을 `justify-between`으로 재구성하여 슬라이더와 재생 버튼이 겹치는 UI 문제를 해결함.
- `bpm_analysis/bpm-analyzer-code2.html`: **Sync** 슬라이더의 크기를 2배로 늘리고, 조정 범위를 **-400ms ~ +400ms**로 확대하여 더욱 정밀하고 폭넓은 타이밍 조정이 가능하도록 개선함.
- `bpm_analysis/bpm-analyzer-code2.html`: 템포(BPM) 변경 시 실시간으로 드럼 비트를 재합성하고 재생 중인 오디오에 즉각 반영하는 로직을 구현함. 재생을 멈추지 않고도 변경된 BPM의 메트로놈 소리를 들을 수 있으며, 파형 시각화 또한 즉시 갱신되도록 처리함.
- `bpm_analysis/bpm-analyzer-code2.html`: 킥 드럼 탐지 기반의 정밀 위상 보정 알고리즘을 도입함. 50Hz 로우패스 필터(LPF)로 저역대를 추출하고, 해당 주파수 대역의 에너지 맥동(Pulse)이 설정된 BPM과 동기화되는 최적의 지점을 찾아 메트로놈 시작 시간(Phase)을 자동으로 설정하도록 개선함.
- `bpm_analysis/bpm-analyzer-code2.html`: 파형 줌(Zoom) 기능을 마우스 커서 위치 기반으로 개선함. 확대/축소 시 마우스가 가리키는 파형의 지점이 화면상의 동일한 위치에 유지되도록 오프셋을 자동 보정하여, 더욱 직관적이고 정밀한 탐색이 가능하도록 함.
- `bpm_analysis/bpm-analyzer-code2.html`: 플레이바(Playhead)가 화면 왼쪽 밖으로 벗어났을 때 이를 알려주는 깜빡이는 화살표 오버레이를 추가함. 오버레이를 클릭하면 현재 재생 중인 위치로 즉시 화면이 이동(Scroll back)하는 복구 기능을 포함함.
- Favicon 404 로그 정리
  - 문제점: 로컬 정적 서버 실행 시 브라우저가 사이트 아이콘 확인을 위해 `/favicon.ico`를 자동 요청했지만, 해당 파일 또는 favicon 설정이 없어 `GET /favicon.ico 404` 로그가 발생함.
  - 해결방안: 각 HTML 문서의 `<head>` 영역에서 기존 프로젝트 로고 파일인 `focustone_logo.svg`를 favicon으로 명시하여 브라우저가 올바른 아이콘 파일을 참조하도록 처리함.
  - 실제 수정 내용: `upload_dashboard/main-code-v6.html`, `bpm_analysis/bpm-analyzer-code2.html`, `drum_beat_maker/drum-beat-maker.html`에 `<link rel="icon" href="focustone_logo.svg" type="image/svg+xml">`를 추가함.
- 재생 중 waveform 왼쪽 pan 제한 현상 수정
  - 문제내용: 일정 구간 재생 후 waveform을 확대한 상태에서 왼쪽으로 pan하면, 사용자가 `currentOffset`을 이전 구간으로 이동시키는 순간 재생 위치가 화면 오른쪽 85% 지점을 넘은 것으로 계산되어 auto-scroll 로직이 `currentOffset`을 다시 오른쪽으로 강제 보정함. 이로 인해 특정 시점부터 왼쪽 pan이 더 이상 진행되지 않는 것처럼 보임.
  - 수정방향: 재생 중 자동 follow 상태와 사용자의 수동 탐색 상태를 분리함. 사용자가 wheel zoom 또는 drag pan을 수행하면 자동 follow를 일시 해제하고, 화면의 follow indicator를 클릭했을 때만 현재 playhead 위치로 다시 복귀하도록 처리함.
  - 실제 수정내용: `bpm_analysis/bpm-analyzer-code2.html`에 `isWaveformAutoFollowEnabled` 상태를 추가하고, auto-scroll 조건에 해당 상태를 반영함. wheel/drag 입력 시 follow를 해제하며, follow indicator 클릭 시 follow를 다시 활성화하고 playhead 중심으로 `currentOffset`을 재계산하도록 수정함. 또한 playhead가 현재 waveform 윈도우의 왼쪽/오른쪽 어느 바깥에 있는지에 따라 `arrow_back`/`arrow_forward`와 안내 문구가 표시되도록 보완함.
- 하단 컨트롤 아이콘 클릭 동작 추가
  - `upload_dashboard/main-code-v6.html`: Master Gain 왼쪽 볼륨 아이콘에 클릭/키보드 접근 가능한 mute 토글 기능을 추가. 아이콘 클릭 시 볼륨 슬라이더 값을 0으로 변경하고, 다시 클릭하면 직전 볼륨 값으로 복원되도록 처리. 슬라이더 배경, `localStorage`, 실제 `gainNode.gain.value`, 아이콘 상태(`volume_up`/`volume_off`)가 함께 갱신되도록 기존 `input` 이벤트 흐름을 재사용함.
  - `bpm_analysis/bpm-analyzer-code2.html`: 하단 Master 아이콘과 Metronome Mix 아이콘에 각각 mute 토글 기능을 추가. Master는 `volume_up`/`volume_off`, Metronome Mix는 `notifications`/`notifications_off`로 상태를 표시하며, 클릭 시 기존 슬라이더 input 이벤트를 발생시켜 실제 gain 값과 슬라이더 UI가 동시에 업데이트되도록 처리.
  - `bpm_analysis/bpm-analyzer-code2.html`: Sync 글자 왼쪽 `sync_alt` 아이콘 클릭 시 Drum Sync Offset 값을 0ms로 리셋하고, `drum-offset-control` 슬라이더와 표시 텍스트, 슬라이더 배경, waveform 시각화가 즉시 갱신되도록 처리.
- Drum Beat Maker 횡스크롤 시 Track Label 위치 이동 문제 수정
  - 문제내용: `drum_beat_maker/components.jsx`의 `TrackRow` 전체 row에는 `px-2`가 적용되어 Track Label이 평상시 왼쪽에서 8px 안쪽에 배치되지만, Track Label 자체는 `sticky left-0`으로 설정되어 있었음. 이 때문에 횡축 스크롤을 시작하면 sticky가 활성화되면서 label이 스크롤 컨테이너의 `left: 0`에 붙어 기존 위치보다 왼쪽으로 살짝 이동하는 현상이 발생함.
  - 수정내용: Track Label의 sticky offset을 row padding과 맞추기 위해 `sticky left-0`을 `sticky left-2`로 변경함. 이를 통해 스크롤 전후 label의 기준 위치가 일관되도록 보정함.
- Drum Beat Maker Bar 표시 및 선택 Track Label 시인성 개선
  - `drum_beat_maker/app.jsx`: PAD 상단 BAR 사각형 오버레이의 테두리 색상을 더 진하게 조정하고, BAR 번호를 일반 숫자에서 로마숫자(`I`, `II`, `III`...)로 변경함. 숫자 폰트도 기존보다 크게(`text-[12px]`) 표시되도록 수정하여 BAR 구분이 더 선명하게 보이도록 처리함.
  - `drum_beat_maker/components.jsx`: 실제 step marker 영역에서 BAR 시작 지점에 표시되던 숫자도 로마숫자로 변경하고, BAR 시작 숫자는 더 크고 진한 cyan 계열로 표시되도록 보완함.
  - `drum_beat_maker/components.jsx`: 선택된 악기 track 전체 오버레이는 기존 흰색 계열 강조로 유지하고, 선택된 악기의 Track Label 배경만 노란색 계열 gradient로 변경함. 초기 투명도가 낮아 시인성이 약해 보여 `rgba(250,204,21,.52)` ~ `rgba(250,204,21,.24)` 수준으로 조정함.
  - `drum_beat_maker/components.jsx`: 선택된 Track Label이 오른쪽 PAD의 Y축 높이와 맞도록 `self-stretch`를 추가함. 이로써 label 배경이 PAD 행 높이와 동일하게 확장되어 선택된 track이 더 안정적인 한 행 단위로 인식되도록 개선함.
- 앱 버전 표시 오류 수정 및 버전 출처 통합
  - 문제내용: `upload_dashboard/main-code-v6.html`, `bpm_analysis/bpm-analyzer-code2.html`, `drum_beat_maker/components.jsx`의 화면 상단 버전 표시가 `v0.9.0`처럼 하드코딩되어 있어 실제 `APP_VERSION` 값(`v0.9.1`)과 다르게 표시됨. 또한 `main-code-v6.html`, `bpm-analyzer-code2.html` 내부에 각각 `const APP_VERSION`이 선언되어 있어 루트 `version.js`와 버전 출처가 중복되는 문제가 있었음.
  - 수정방향: 루트 `version.js`를 단일 버전 출처로 사용하고, 각 화면은 해당 파일에서 노출하는 `window.APP_VERSION` 값을 읽어 표시하도록 통일함. HTML별 inline `const APP_VERSION` 선언은 제거하여 중복 선언과 버전 불일치 가능성을 줄임.
  - 수정내용: `version.js`를 `window.APP_VERSION = 'v0.9.1';` 형태로 정리하고, `main-code-v6.html`, `bpm-analyzer-code2.html`, `drum-beat-maker.html`에서 `../version.js`를 로드하도록 추가함. `main-code-v6.html`과 `bpm-analyzer-code2.html`은 `.app-version` 요소에 `window.APP_VERSION`을 주입하도록 변경했고, `drum_beat_maker/components.jsx`는 React 렌더링에서 `appVersion` 값을 표시하도록 수정함. 초기 수정 후 Drum Beat Maker에서 버전 표시가 사라지는 문제가 확인되어, 전역 값을 명확히 `window.APP_VERSION`으로 노출하고 컴포넌트에서 이를 안정적으로 읽도록 보완함.

## 2026-05-28 HTML 점검 및 수정
- 점검 범위: `backup/` 폴더를 제외한 `upload_dashboard/main-code-v6.html`, `bpm_analysis/bpm-analyzer-code2.html`, `drum_beat_maker/drum-beat-maker.html`.
- 주요 발견 사항: 업로드 대시보드의 최근 파일 기본 UI에 잘못 닫힌 `div`가 있었고, BPM 분석 화면의 최고 후보 BPM 표시가 특수문자/템플릿 문자열 문제로 환경에 따라 깨져 보일 수 있었다.
- 주요 발견 사항: BPM 분석 HTML에 Material Symbols 폰트 링크가 중복되어 있었고, 여러 아이콘 버튼에 `type="button"` 또는 접근성 라벨이 부족했다.
- 수정 내역: `upload_dashboard/main-code-v6.html`에서 파일 선택 버튼의 인라인 `onclick`을 제거하고 `addEventListener`로 연결했다. 최근 파일 렌더링에는 `escapeHtml()`을 추가해 파일명/포맷/크기/BPM 값이 HTML로 삽입될 때 안전하게 표시되도록 했다.
- 수정 내역: `bpm_analysis/bpm-analyzer-code2.html`에서 중복 Material Symbols 링크를 제거하고, BPM 후보 클릭 처리를 `.onclick` 대입 대신 `addEventListener`로 변경했다. 최고 후보 라벨은 `123 (best) BPM` 형식으로 표시되도록 정리했다.
- 수정 내역: `drum_beat_maker/drum-beat-maker.html`의 문서 제목 구분자를 ASCII 하이픈으로 바꿔 콘솔/편집기 인코딩 환경에서도 깨져 보이지 않도록 했다.
- 공통 수정: 세 HTML의 주요 버튼에 `type="button"`을 추가하고, 아이콘 전용 버튼에는 `aria-label`을 보강했다.
- 검증: 백업 제외 3개 HTML에서 인라인 이벤트 핸들러, `.onclick` 대입, 깨진 `??/div`, BPM 라벨 깨짐, Material Symbols 중복 여부를 재검색했고, 인라인 스크립트 파싱 검사를 통과했다.
